============
Installation
============

At the command line::

    $ easy_install multyvacinit

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv multyvacinit
    $ pip install multyvacinit
